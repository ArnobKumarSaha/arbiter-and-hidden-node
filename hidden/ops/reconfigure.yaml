apiVersion: ops.kubedb.com/v1alpha1
kind: MongoDBOpsRequest
metadata:
  name: reconfigure
  namespace: demo
spec:
  type: Reconfigure
  databaseRef:
    name: mongo
  configuration:
    replicaSet:
      configSecret:
        name: new-conf
    arbiter:
      configSecret:
        name: new-conf
    hidden:
      configSecret:
        name: prev-conf